# scope
- 只考虑静态链接

# use
## wish
- 提供CreateFunction
- 顶多在class前后加些宏
- 考虑后续修改扩展容易，不要每次扩展就要把所有代码改一遍
## detail
AModule.h
class AModule : public IModule, public SomeInterface
{
public:
	SOME_IN_CLASS_MACRO(AModule);
public:
	void SomeMethod0();
	void SomeMethod1();
}
SOME_OUT_OF_CLASS_MACRO(AModule);


AModule.cpp
void AModule::SomeMethod0()
{}
void AModule::SomeMethod1()
{}
SOME_IMP_CLASS_MACRO(AModule);

# imp