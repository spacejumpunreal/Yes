- doing
	- SimpleDX12Renderer
		- execute renderpass
			- TestDriver
				- depth buffer 使用
				- normal transform
				- 两个drawcall会有问题
				- 感觉camera移动有点怪
			- RenderPass::ExecutePass
				- DescriptorHeap Handle顺序调整，第一个应该是RTV,然后是SRV
				- SetXXXConstantBuffer的方式修改
					- 暴露allocate接口，返回handle，set handle
				- ConstantBufferManager申请>= 64kb buffer resource trunk
				- render command: texture
					- allocate temp Descriptor heap space
					- 如何标识使用了不同的descriptorheap，可能要切换descriptorheap，同一个drawcall内不能换
					- copy desciptor heap to temp table
					- SetGraphicsRootDescriptorTable
				- render command: RenderTarget rw
					- barrier
			- EndFrame
		